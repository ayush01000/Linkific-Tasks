# FastAPI Inventory Management API

A beginner-friendly REST API demonstrating FastAPI routes, Pydantic validation,
CRUD operations, error handling, routers, CORS, automatic documentation, and
testing.

This first version stores products in memory, so no database setup is required.
The data resets whenever the development server restarts.


